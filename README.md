# Learning VLLM

## 1. Environment Setup

```bash
$ uv venv -p 3.12
$ source .venv/bin/activate
$ uv pip install -v .
```

For development, you can also install this package in editable mode:

```bash
$ uv pip install -v -e .
```